{
  "name": "missneo",
  "version": "0.0.0",
  "description": "neo for misskey",
  "main": "missneo.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/inject-version.mjs",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.15.1",
      "onFail": "download"
    }
  },
  "type": "module",
  "dependencies": {
    "typescript": "^7.0.2"
  }
}
